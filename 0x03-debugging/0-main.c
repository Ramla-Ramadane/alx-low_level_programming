#include "main.h"

/**
 * main - entry point
 * tests function that prints if integer is negative or positive numbers
 * Return: 0
 */
int main(void)
{
	int n;

	n = 0;
	positive_or_negative(n);

	return (0);
}
