#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: parameter to be checked
 * Return: 0
 */
int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}

		return (0);
}
