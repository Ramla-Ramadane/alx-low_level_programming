#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: parameter checked
 * Return: a
 */
int _strlen(char *s)
{
	int a = strlen(s);

	return (a);
}
